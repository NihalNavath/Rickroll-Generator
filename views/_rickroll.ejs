<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://nihalnavath.com/global.css">

    <title>
        <%= title %>
    </title>

    <meta name="twitter:card" content="summary">
    <meta property="og:title" content="<%= title %>">
    <meta property="twitter:title" content="<%= title %>">

    <meta name="description" content="<%= description %>" />

    <meta property="og:description" content="<%= description %>" />
    <meta property="twitter:description" content="<%= description %>" />

    <meta property="og:image" content="<%= ImgUrl %>" />
    <meta property="twitter:image" content="<%= ImgUrl %>" />

    <style>
        body,
        html {
            height: 100%;
        }

        #bg {
            background-image: url("/img/blog.png");

            /* Full height */
            height: 100%;

            /* Center and scale the image nicely */
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;

            /* Add the blur effect */
            filter: blur(10px);
            -webkit-filter: blur(10px);
        }

        #box {
            width: 50%;
            background-color: lightgray;
            color: black;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -110%);
            backdrop-filter: blur(5px);
        }

        #box button {
            width: 100%;
            height: 5vh;
            font-size: 15px;
        }

        #box p {
            font-size: 20px;
            letter-spacing: 1.2px;
        }

        #title {
            padding: 10px;
        }

        /* rickroll  */
        body,
        html {
            color: white;
            margin: 0;
            background-color: #212529 !important;
            font-family: "Montserrat", sans-serif;
        }

        a {
            color: blue;
        }

        /* rickroll */
        body,
        html {
            color: white;
            margin: 0;
            background-color: #212529 !important;
        }

        .btn {
            background-color: #198754;
            color: #fff;
            margin: .25rem .125rem;
            cursor: pointer;
            border-color: #fff;
            font-size: 1rem;
            font-weight: 400;
            line-height: 1.5;
        }

        .acc-btn {
            background-color: #198754;
            color: #fff;
            cursor: pointer;
        }

        @media only screen and (max-width: 1000px) {
            video {
                width: 100%;
            }

            #box {
                width: 100%;
            }
        }
    </style>
</head>

<body class="text-center">
    <div id="bg"></div>
    <!-- not using dialog element as it might not work for older devices or browsers -->
    <div id="box">
        <div id="title">
            <p>We would like to show you some cookies! 🍪</p>
            <a>see preferences</a>
        </div>
        <button class="acc-btn">Accept</button>
        <button>Decline</button>
    </div>

    <div id="rickroll" style="display: none;">
        <center>
            <h1>
                <%= text %>
            </h1>
            <p>Wanna make your own custom rickrolls?
                <button class="btn" href="https://news.rr.nihalnavath.com?ref=rickrolled">click here!</button>
            </p>
            <video id="video" width="50%" height="50%" autoplay loop>
                <source src="/videos/rickroll_60.mp4" type="video/mp4">
            </video>
            <p>feedback on the new look? <a href="https://discord.gg/3EP6bMK3YA">join our discord</a></p>
        </center>
    </div>

    <script>
        const btnCallback = () => {
            const toHide = document.getElementById("box");
            const toShow = document.getElementById("rickroll");
            const video = document.getElementById("video");
            const bg = document.getElementById("bg");
            toHide.style.display = "none";
            bg.style.display = "none";
            toShow.style.display = "block";

            video.play();
        }

        document.getElementsByTagName("body")[0].addEventListener("click", btnCallback);
    </script>
</body>

</html>